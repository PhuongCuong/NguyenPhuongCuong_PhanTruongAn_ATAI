import React from 'react';
import { View } from 'react-native';
import { useRoute } from "@react-navigation/native";
const Home = () => {
    return (
        <View>
            Hello home
        </View>
    );
};

export default Home;